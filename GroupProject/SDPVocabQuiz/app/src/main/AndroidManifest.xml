<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seclass.gatech.edu.sdpvocabquiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginManagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".QuizzActivityDefs" />
        <activity android:name=".QuizzActivityIncorrect" />
        <activity android:name=".SelectPracticeQuizActivity" />
        <activity android:name=".PracticeActivity" />
        <activity android:name=".CompletedPracticeActivity" />
        <activity android:name=".SelectReviewQuizActivity" />
        <activity android:name=".QuizStatisticsActivity"></activity>
    </application>

</manifest>